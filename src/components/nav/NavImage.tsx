import styled from 'styled-components';

const NavImage = styled.img`
    width: 100%;
    max-width: 90px;
    &.nav-side-img{
        width:27px;
        height: 26px;
    }
    &.nav-side-img:hover{
        cursor: pointer;
    }
`
export default NavImage;
