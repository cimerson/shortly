import styled from 'styled-components';
import { theme }  from '../../theme';

const NavModalLine = styled.hr`
    border:none;
    height: 0.5px;
    background-color: ${theme.colors.neutral.gray};
`
export default NavModalLine;
